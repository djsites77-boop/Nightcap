# Host Home / Dashboard

Overrides MASTER.md:

- Hero = largest night-cap **gauge** for the listing that needs the most attention
- Secondary = big status numbers (attention, MAT due) — not a spreadsheet
- Always show **area chart** of nights booked (last 6 months)
- Property tiles each include a mini gauge + status pill in plain language (“Looking good”)
- Mobile: bottom tabs; no sidebar
